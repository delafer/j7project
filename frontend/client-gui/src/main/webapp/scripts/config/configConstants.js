define(['./module'],
            function(constants) {
            'use strict';

            constants.constant(
                    'production', true);

            constants.constant(
                    'showSearchParams', false);

            constants.constant(
                    'spinner', true);

        });

